@model IEnumerable<MyVideoTraining.Entities.AssignmentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AssignmentTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AssignmentTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignmentTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignmentTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignmentTypeId })
        </td>
    </tr>
}

</table>
